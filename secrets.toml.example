# Copie este arquivo para dentro de uma pasta chamada ".streamlit"
# (crie essa pasta na mesma pasta do app.py, se ela não existir), e
# renomeie a cópia para "secrets.toml" (sem o ".example" no final).
#
# Depois, troque a linha abaixo pela sua Connection String real do Supabase
# (veja o passo a passo no README.md, seção "Colocar no ar para várias
# pessoas usarem").

SUPABASE_DB_URL = "postgresql://postgres:SUA_SENHA_AQUI@SEU_HOST_AQUI:5432/postgres"
